#include <stdio.h>

int main()
{
	char option;
	int age;
	float height;

	printf("The size of char is %zu\n", sizeof(char));
	printf("The size of int is %zu\n", sizeof(int));
	printf("The float is %zu\n", sizeof(float));

	return 0;
}
