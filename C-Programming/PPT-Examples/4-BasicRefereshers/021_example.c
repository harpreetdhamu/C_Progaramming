#include <stdio.h>

int main()
{
	int num1 = 1, num2 = 0,a=1,b=-1,c=1,d=6,e=8,x=-10,y=0;
x=!!x;
y=!!y;
/*

	if (++num1 || num2++)
	{
		printf("num1 is %d num2 is %d\n", num1, num2);
	}
	num1 = 1, num2 = 0;
	if (num1++ && ++num2)
	{
		printf("num1 is %d num2 is %d\n", num1, num2);
	}
	else
	{
		printf("num1 is %d num2 is %d\n", num1, num2);
	}*/
//x= a++ &&  ++b || c--  || ++d && --e;
		printf("%d %d \n", x,y);
	return 0;
}
